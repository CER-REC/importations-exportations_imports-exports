let assert = require('assert')

describe('Arrayz', function() {
  describe('#indexOf()z', function() {
    it('should return -1 when the value is not present ...', function() {
      assert.equal(3, [1,2,3, 4].indexOf(4))
    })
  })
})

